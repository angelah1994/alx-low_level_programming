#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fera computers. I fera the lack of them - Isaac Asiov";
	print_rev(str);
		return (0);
}
